import { PropertyAccessGuard } from './property-access.guard';

describe('PropertyAccessGuard', () => {
  it('should be defined', () => {
    expect(new PropertyAccessGuard()).toBeDefined();
  });
});
